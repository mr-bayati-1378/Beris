# 🔐 راهنمای سیستم ورود مدیریتی بریس

## 📋 کاربران تست ایجاد شده

سیستم مدیریت بریس دارای 5 نقش مختلف است که هر کدام داشبورد مخصوص خود را دارند:

### 👤 اطلاعات لاگین کاربران تست

| نقش | شماره تلفن | رمز عبور | داشبورد |
|-----|------------|-----------|----------|
| مدیر کل | `09111111111` | `123456` | `/admin` |
| مدیر فروش | `09222222222` | `123456` | `/admin-sales` |
| مدیر تامین | `09333333333` | `123456` | `/admin-supply` |
| مدیر انبار | `09444444444` | `123456` | `/admin-warehouse` |
| مدیر مالی | `09555555555` | `123456` | `/admin-finance` |

## 🌟 ویژگی‌های سیستم لاگین

### ✅ هدایت خودکار
- هر کاربر پس از ورود به داشبورد مخصوص نقش خود هدایت می‌شود
- مدیر کل به همه بخش‌ها دسترسی دارد
- سایر مدیران فقط به بخش مربوط به خود دسترسی دارند

### ✅ امنیت
- رمز عبور هش شده در دیتابیس ذخیره می‌شود
- Session-based authentication
- محدودیت دسترسی بر اساس نقش

### ✅ طراحی مدرن
- رابط کاربری زیبا و مدرن
- طراحی responsive برای موبایل
- انیمیشن‌های نرم
- رنگ‌بندی منحصربه‌فرد برای هر نقش

## 🎯 نحوه تست

### 1. راه‌اندازی سرور
```bash
npm run dev
```

### 2. ورود به صفحه لاگین
بروید به: `http://localhost:3000/admin/login`

### 3. تست هر نقش
#### مدیر کل (admin)
- شماره: `09111111111`
- رمز: `123456`
- انتظار: هدایت به `/admin`
- ویژگی: دسترسی به همه بخش‌ها

#### مدیر فروش (sales)
- شماره: `09222222222` 
- رمز: `123456`
- انتظار: هدایت به `/admin-sales`
- ویژگی: داشبورد آبی با آمار فروش

#### مدیر تامین (supply)
- شماره: `09333333333`
- رمز: `123456`
- انتظار: هدایت به `/admin-supply`
- ویژگی: داشبورد سبز با آمار تامین

#### مدیر انبار (warehouse)
- شماره: `09444444444`
- رمز: `123456`
- انتظار: هدایت به `/admin-warehouse`
- ویژگی: داشبورد نارنجی با آمار انبار

#### مدیر مالی (finance)
- شماره: `09555555555`
- رمز: `123456`
- انتظار: هدایت به `/admin-finance`
- ویژگی: داشبورد بنفش با آمار مالی

## 🔧 ساختار فنی

### API Endpoints
- `POST /api/admin/auth/login` - ورود مدیریتی
- `GET /api/admin/dashboard?role=<role>` - دریافت آمار هر نقش
- `GET /api/admin/navigation` - منوهای مربوط به نقش

### بررسی دسترسی
- Middleware: `src/middleware.ts`
- Admin Roles: `src/lib/admin-roles.ts`
- Permission Check: در هر API endpoint

### Layout های جداگانه
- `/admin/layout.tsx` - مدیر کل
- `/admin-sales/layout.tsx` - مدیر فروش
- `/admin-supply/layout.tsx` - مدیر تامین
- `/admin-warehouse/layout.tsx` - مدیر انبار
- `/admin-finance/layout.tsx` - مدیر مالی

## 🛠️ اگر مشکلی داشتید

### بازسازی دیتابیس
```bash
cd prisma
npx prisma db push
```

### بازسازی نقش‌ها
```bash
export DATABASE_URL="mysql://beris_user:beris_password@localhost:3306/beris"
node prisma/seed-admin-roles.js
```

### بازسازی کاربران تست
```bash
export DATABASE_URL="mysql://beris_user:beris_password@localhost:3306/beris"
node prisma/seed-test-admins.js
```

### پاک کردن cache Next.js
```bash
rm -rf .next
npm run dev
```

## ✨ تغییرات اعمال شده

1. **نقش‌های مدیریتی جداگانه**: هر نقش داشبورد مخصوص خود دارد
2. **هدایت خودکار**: بر اساس نقش کاربر پس از لاگین
3. **API مخصوص هر نقش**: آمارهای متفاوت برای هر داشبورد
4. **طراحی منحصربه‌فرد**: رنگ‌بندی و آیکون‌های مخصوص هر بخش
5. **امنیت**: محدودیت دسترسی بر اساس نقش

## 🎨 رنگ‌بندی داشبوردها

- **مدیر کل**: آبی تیره (#1e40af)
- **فروش**: آبی (#2563eb) 
- **تامین**: سبز (#059669)
- **انبار**: نارنجی (#d97706)
- **مالی**: بنفش (#7c3aed)

---

💡 **نکته**: تمام داشبوردها responsive هستند و در موبایل و تبلت به خوبی کار می‌کنند. 

# راهنمای ورود مدیران سیستم Beris

## اطلاعات لاگین مدیران

> **توجه**: تمام مدیران از همین صفحه لاگین وارد می‌شوند و بر اساس نقش خود به داشبورد مناسب منتقل می‌شوند.

### 🔵 مدیران کل (Admin)
- **علیرضا بیاتی**
  - نام کاربری: `mrbayati1378`
  - رمز عبور: `Alireza021`
  - داشبورد: `/admin/`

- **دکتر ندائی**
  - نام کاربری: `dr.nedaey`
  - رمز عبور: `12345678`
  - داشبورد: `/admin/`

### 🟢 مدیر تامین (Supply Manager)
- **مدیر تامین**
  - نام کاربری: `supply`
  - رمز عبور: `12345678`
  - داشبورد: `/admin-supply/`

### 🔵 مدیر فروش (Sales Manager)
- **امیرحسین**
  - نام کاربری: `amirhossein`
  - رمز عبور: `12345678`
  - داشبورد: `/admin-sales/`

### 🟠 مدیر انبار (Warehouse Manager)
- **کاوه**
  - نام کاربری: `kaveh`
  - رمز عبور: `12345678`
  - داشبورد: `/admin-warehouse/`

### 🟣 مدیر مالی (Finance Manager)
- **متین**
  - نام کاربری: `matinmfp`
  - رمز عبور: `12345678`
  - داشبورد: `/admin-finance/`

## نحوه ورود

1. به آدرس `/admin/login` بروید
2. نام کاربری و رمز عبور خود را وارد کنید
3. سیستم شما را به داشبورد مناسب نقش‌تان هدایت می‌کند

## ویژگی‌ها

✅ **یک سیستم لاگین یکپارچه** - همه مدیران از همین صفحه وارد می‌شوند  
✅ **تشخیص خودکار نقش** - هر مدیر به داشبورد مناسب خود منتقل می‌شود  
✅ **امنیت بالا** - رمزهای عبور encrypt شده ذخیره می‌شوند  
✅ **جلسه محفوظ** - Session مدیران تا 24 ساعت فعال است  

## داشبوردها

- **🔵 داشبورد مدیر کل**: مدیریت کامل سیستم
- **🔵 داشبورد فروش**: مدیریت سفارشات، مشتریان و فاکتورها
- **🟢 داشبورد تامین**: مدیریت محصولات، تامین‌کنندگان و خرید
- **🟠 داشبورد انبار**: مدیریت موجودی و حمل و نقل
- **🟣 داشبورد مالی**: مدیریت پرداخت‌ها و گزارشات مالی

## نکات مهم

⚠️ فقط نام کاربری و رمز عبور کافی است - نیازی به شماره تلفن یا ایمیل نیست  
⚠️ در صورت فراموشی رمز عبور، با مدیر کل تماس بگیرید  
⚠️ برای امنیت، بعد از کار حتماً خروج کنید  

---

*تاریخ آخرین به‌روزرسانی: ۲۰ دی ۱۴۰۳* 